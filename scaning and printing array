#include <stdio.h>

int main() {
    int size;
    //int arr[size];
    printf("what is the size of array\n");

    scanf("%d",&size);

    int arr[size];
    printf("Enter the array..\n");
        //scanf("%d",&size);
    for(int i=0;i<size;i++){
        scanf("%d",&arr[i]);
    }
    printf("the arrray is ");
    for(int i=0;i<size;i++){
        printf("%d ",arr[i]);
    }
    printf("\n");
    
    return 0;
}
